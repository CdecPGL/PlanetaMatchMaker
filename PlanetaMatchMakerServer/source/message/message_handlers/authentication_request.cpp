#include "authentication_request.hpp"

void pgl::authentication_request::handle_message(const authentication_request_message& message,
	const std::shared_ptr<server_data>& server_data, boost::asio::yield_context& yield) {
	
}
